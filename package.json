{
  "name": "nuxt-app",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build:css": "sass --no-source-map -I node_modules assets/main.scss assets/compiled.css",
    "build": "yarn build:css && nuxt generate",
    "dev": "nuxt dev",
    "generate": "nuxt generate"
  },
  "devDependencies": {
    "@types/node": "^18",
    "nuxt": "^3.5.2",
    "sass": "^1.63.3"
  },
  "dependencies": {
    "bulma": "^0.9.4",
    "dayjs": "^1.11.8",
    "nuxt-gtag": "^0.5.7"
  }
}
